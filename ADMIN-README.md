# 🔐 لوحة التحكم Admin Panel - مطعم ماسة

## 📋 نظرة عامة

تم بناء لوحة تحكم كاملة لإدارة المنتجات والتصنيفات مع جميع عمليات CRUD:
- ✅ **Create** - إضافة منتجات وتصنيفات جديدة
- ✅ **Read** - عرض وفلترة المنتجات
- ✅ **Update** - تعديل المنتجات والتصنيفات
- ✅ **Delete** - حذف المنتجات والتصنيفات

## 🚀 كيفية الدخول

### 1. الوصول للوحة التحكم:
```
http://localhost:5173/admin/login
```

### 2. بيانات تسجيل الدخول:
- **كلمة المرور الافتراضية**: `masa2025`

### 3. بعد تسجيل الدخول:
سيتم تحويلك تلقائياً إلى: `/admin/dashboard`

---

## 🎯 الميزات الرئيسية

### 📊 لوحة المعلومات (Dashboard)
عند الدخول، سترى:
- **إجمالي التصنيفات**: عدد التصنيفات المتاحة
- **إجمالي المنتجات**: عدد المنتجات الكلي
- **متوسط المنتجات**: متوسط عدد المنتجات لكل تصنيف

### 📦 إدارة المنتجات

#### إضافة منتج جديد:
1. اختر **التصنيف** من القائمة المنسدلة
2. املأ البيانات:
   - **اسم المنتج** (إجباري)
   - **السعر** (اختياري - بالجنيه المصري)
   - **رابط الصورة** (اختياري)
   - **الوصف** (اختياري)
   - **صور متعددة** (اختياري - للتبديل التلقائي)
3. اضغط **إضافة المنتج**

#### صور متعددة متحركة:
يمكنك إضافة عدة روابط صور (كل رابط في سطر منفصل) للحصول على:
- 🔄 تبديل تلقائي كل 5 ثواني
- مثال:
```
https://files.catbox.moe/image1.jpg
https://files.catbox.moe/image2.jpg
https://files.catbox.moe/image3.jpg
```

#### تعديل منتج:
1. ابحث عن المنتج في القائمة
2. اضغط زر **تعديل**
3. سيظهر نموذج التعديل في الأعلى
4. عدّل البيانات المطلوبة
5. اضغط **تحديث المنتج**

#### حذف منتج:
1. ابحث عن المنتج
2. اضغط زر **حذف**
3. أكد الحذف

#### فلترة المنتجات:
- استخدم القائمة المنسدلة لعرض منتجات تصنيف معين
- أو اختر "جميع التصنيفات" لعرض الكل

---

### 🗂️ إدارة التصنيفات

#### إضافة تصنيف جديد:
1. انتقل لتبويب **إدارة التصنيفات**
2. اضغط **+ إضافة تصنيف جديد**
3. املأ البيانات:
   - **المعرف (ID)**: بالإنجليزية فقط (مثال: `desserts`, `drinks`)
   - **اسم التصنيف**: بالعربية (مثال: "الحلويات")
   - **رابط الصورة**: صورة التصنيف
4. اضغط **إضافة**

#### تعديل تصنيف:
1. اضغط زر **تعديل** على التصنيف
2. عدّل **الاسم** أو **رابط الصورة**
3. اضغط **تحديث**

⚠️ **ملاحظة**: لا يمكن تعديل الـ ID بعد إنشاء التصنيف

#### حذف تصنيف:
1. اضغط زر **حذف**
2. أكد الحذف

⚠️ **شرط مهم**: لا يمكن حذف تصنيف يحتوي على منتجات!
- احذف المنتجات أولاً، ثم احذف التصنيف

---

## 💾 تخزين البيانات

### LocalStorage:
- جميع التعديلات تُحفظ تلقائياً في **localStorage**
- المفتاح: `menuCategories`
- البيانات تبقى حتى بعد إغلاق المتصفح
- المفتاح: `adminToken` (للـ authentication)

### إعادة التعيين:
إذا أردت الرجوع للبيانات الأصلية:
1. اضغط زر **إعادة تعيين** في الهيدر
2. أكد العملية
3. سيتم حذف جميع التعديلات والعودة للإعدادات الافتراضية

---

## 🔒 الأمان

### Authentication:
- ✅ نظام تسجيل دخول بسيط
- ✅ Protected Routes (لا يمكن الوصول للـ dashboard بدون تسجيل دخول)
- ✅ Token محفوظ في localStorage

### تغيير كلمة المرور:
لتغيير كلمة المرور الافتراضية، عدّل في:
```javascript
// src/contexts/AdminContext.jsx
const login = (password) => {
  if (password === 'كلمة_المرور_الجديدة') {
    // ...
  }
}
```

⚠️ **للإنتاج**: استخدم backend حقيقي مع تشفير وقاعدة بيانات!

---

## 🎨 الواجهة

### الألوان:
- **Primary**: ذهبي (brand-primary)
- **Success**: أخضر (للإضافة)
- **Info**: أزرق (للتعديل)
- **Danger**: أحمر (للحذف)

### التصميم:
- ✅ Responsive (متجاوب مع جميع الشاشات)
- ✅ RTL Support (يدعم العربية بالكامل)
- ✅ Modern UI مع Tailwind CSS
- ✅ Smooth transitions وanimations

---

## 📱 الأزرار الرئيسية

### في الهيدر:
- **عرض الموقع**: العودة للصفحة الرئيسية
- **إعادة تعيين**: حذف جميع التعديلات
- **تسجيل خروج**: الخروج من لوحة التحكم

### في Dashboard:
- **إدارة المنتجات**: تبويب المنتجات
- **إدارة التصنيفات**: تبويب التصنيفات

---

## 🔄 التكامل مع الموقع

### التحديث التلقائي:
- أي تغيير في لوحة التحكم يظهر **فوراً** في الموقع
- لا حاجة لإعادة تحميل الصفحة
- جميع الصفحات (الرئيسية، المنيو) تستخدم نفس البيانات

### Context API:
نستخدم `AdminContext` لمشاركة البيانات:
```javascript
const { categories } = useAdmin()
```

---

## 📊 هيكل البيانات

### التصنيف (Category):
```javascript
{
  id: 'trays',           // معرف فريد
  name: 'صواني',         // اسم التصنيف
  image: '/images/...',  // صورة التصنيف
  items: [...]           // المنتجات
}
```

### المنتج (Product):
```javascript
{
  name: 'كبسة',              // اسم المنتج (إجباري)
  price: 1100,               // السعر (اختياري)
  desc: 'وصف...',           // الوصف (اختياري)
  image: 'https://...',      // صورة واحدة (اختياري)
  images: ['url1', 'url2']   // صور متعددة (اختياري)
}
```

---

## 🚧 للتطوير المستقبلي

### ميزات مقترحة:
1. **Backend API**: 
   - استخدام Supabase أو Firebase
   - تشفير كلمات المرور
   - Multi-user support

2. **رفع الصور**:
   - Upload images مباشرة
   - بدلاً من روابط خارجية

3. **الإحصائيات**:
   - أكثر المنتجات مبيعاً
   - تقارير مفصلة

4. **إدارة الطلبات**:
   - نظام طلبات متكامل
   - تتبع الطلبات

5. **Multi-language**:
   - دعم لغات إضافية

---

## 🐛 حل المشاكل

### المشكلة: لا تظهر التعديلات في الموقع
**الحل**: تأكد من:
1. استخدام `useAdmin()` في الصفحات
2. عدم استيراد `categories` مباشرة من `menu.js`

### المشكلة: تم حذف البيانات عن طريق الخطأ
**الحل**: 
1. اضغط زر **إعادة تعيين**
2. أو امسح localStorage من Developer Tools

### المشكلة: نسيت كلمة المرور
**الحل**:
1. افتح Developer Tools (F12)
2. Application → Local Storage
3. احذف `adminToken`
4. ادخل بكلمة المرور الافتراضية: `masa2025`

---

## ✅ الخلاصة

تم بناء admin panel كامل ومتكامل مع:
- ✅ جميع عمليات CRUD
- ✅ واجهة سهلة وجميلة
- ✅ تخزين محلي
- ✅ حماية بكلمة مرور
- ✅ متجاوب مع جميع الأجهزة
- ✅ تكامل كامل مع الموقع

**جاهز للاستخدام الفوري! 🎉**
